/** IDs de rol alineados con Next (tabla Roles). */
export enum EnumRolUsuario {
  SUPER_ADMIN = 1,
  DESARROLLADOR = 2,
  ADMIN_SISTEMA = 3,
  JEFE_MONITOREO = 4,
  MONITOREO = 5,
  CLIENTE = 6,
  OPERADOR = 7,
}
